pyyaml>=6.0
requests>=2.31
PySocks>=1.7.1
rich>=13.7
typer>=0.12
questionary>=2.0
textual>=0.58
# Dev-only tooling (pytest, ruff, coverage) lives in the pyproject "dev" extra:
#   pip install -e .[dev]
# It is intentionally NOT installed by the venv bootstrap, which only needs the
# runtime deps above.
